{
  "name": "js_solar_assistant",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "scripts": {
    "build": "vite build && vite build -c vite.config.api.js"
  }
}
