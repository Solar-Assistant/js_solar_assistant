{
  "name": "js_solar_assistant",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "happy-dom": "^20.14.0",
    "vite": "^5.0.0",
    "vitest": "^3.2.7"
  },
  "scripts": {
    "build": "vite build && vite build -c vite.config.api.js && vite build -c vite.config.oauth.js",
    "test": "vitest run"
  }
}
